% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/string.R
\name{string_network}
\alias{string_network}
\title{The interaction network within a set of genes}
\usage{
string_network(
  symbols,
  species = STRING_HUMAN,
  required_score = STRING_MIN_SCORE,
  reconcile = TRUE,
  ...
)
}
\arguments{
\item{symbols}{Gene symbols. At least two, since one gene has no network.}

\item{species}{An NCBI taxon id. Defaults to human.}

\item{required_score}{STRING's 0-1000 combined-score threshold.}

\item{reconcile}{Whether to fetch the identifier map and translate edge
endpoints back into your symbols. Costs one extra request and is skipped
automatically when there are no edges to translate.}

\item{...}{Passed to \code{\link[biohttp:get_json]{biohttp::get_json()}}, for example \code{throttle}.}
}
\value{
A biohttp envelope whose \code{data} is a list of \code{edges} (see
\code{\link[=string_parse_network]{string_parse_network()}}), \code{queried}, \code{n_query}, \code{truncated}, and
\code{n_dropped}.
}
\description{
High-confidence edges among the genes you asked about, with the endpoints
reconciled back to your symbols.
}
\section{Knowing what was asked}{

The result carries \code{queried}, the exact symbol set STRING was asked about
after the 500-identifier cap, plus \code{truncated} and \code{n_dropped}. That is
what lets a caller tell a gene measured to have no partners from a gene that
was never sent. Reporting the second as the first would invent a negative
result.
}

\examples{
\donttest{
res <- string_network(c("TP53", "NF1", "EGFR"))
biohttp::body_or_null(res)$edges
}

}
