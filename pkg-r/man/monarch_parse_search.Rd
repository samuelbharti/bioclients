% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/monarch.R
\name{monarch_parse_search}
\alias{monarch_parse_search}
\title{Turn a Monarch search response into a table}
\usage{
monarch_parse_search(body)
}
\arguments{
\item{body}{A parsed Monarch \code{search} response.}
}
\value{
A tibble of \code{id}, \code{name}, \code{category}, \code{description}, \code{taxon}, and
\code{source_url}, one row per match. \code{NULL} when there are none.
}
\description{
Pure.
}
\section{Read the taxon}{

A gene symbol is not unique across species in Monarch, and a search returns
the orthologs alongside the human gene under the same name. \code{taxon} is the
only column that tells them apart. It is \code{NA} for a disease or a phenotype,
which are not species-scoped.
}

\examples{
body <- list(items = list(list(
  id = "HGNC:3603",
  name = "FBN1",
  category = "biolink:Gene",
  full_name = "fibrillin 1",
  in_taxon_label = "Homo sapiens"
)))
monarch_parse_search(body)

}
