% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gnomad.R
\name{gnomad_parse_variants}
\alias{gnomad_parse_variants}
\title{Turn an aliased gnomAD variant response into a table}
\usage{
gnomad_parse_variants(body, variant_ids)
}
\arguments{
\item{body}{A parsed gnomAD GraphQL response body.}

\item{variant_ids}{The ids that were queried, in the order asked.}
}
\value{
A tibble with one row per entry in \code{variant_ids}, same order. See
\code{\link[=gnomad_parse_variant]{gnomad_parse_variant()}} for the columns.
}
\description{
Pure. The batched query uses aliases \code{v1}, \code{v2}, and so on, so rows are
mapped back by alias index rather than by the echoed id. A variant gnomAD
has not seen comes back as \code{null} under its alias, with a
"Variant not found" entry in \code{errors}, and becomes a row of \code{NA}.
}
\examples{
body <- list(data = list(
  v1 = list(variant_id = "17-7676154-G-C", exome = list(af = 0.72)),
  v2 = NULL
))
gnomad_parse_variants(body, c("17-7676154-G-C", "17-7676154-G-GTTTTT"))

}
\references{
Chen et al. (2024). A genomic mutational constraint map using variation
in 76,156 human genomes. Nature 625(7993), 92-100.
\doi{10.1038/s41586-023-06045-0}

Service documentation: \url{https://gnomad.broadinstitute.org/}
}
