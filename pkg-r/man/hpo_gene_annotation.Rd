% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hpo.R
\name{hpo_gene_annotation}
\alias{hpo_gene_annotation}
\title{HPO's annotation for a gene}
\usage{
hpo_gene_annotation(entrez, ...)
}
\arguments{
\item{entrez}{An NCBI Gene id, for example \code{7157}. See the note on the
lookup key in the file header: nothing else works here.}

\item{...}{Passed to \code{\link[biohttp:get_json]{biohttp::get_json()}}, for example \code{throttle}.}
}
\value{
A biohttp envelope whose \code{data} is a list of two tibbles, \code{diseases}
and \code{phenotypes}, either of which may be \code{NULL}. Two tables rather than one,
because they are independent arrays in the response and joining them would
invent a disease-to-phenotype mapping the response does not carry. It also
carries \code{source_url}. See \code{\link[=hpo_parse_diseases]{hpo_parse_diseases()}} and
\code{\link[=hpo_parse_phenotypes]{hpo_parse_phenotypes()}}.
}
\description{
HPO's annotation for a gene
}
\examples{
\donttest{
biohttp::body_or_null(hpo_gene_annotation(7157))$diseases
}

}
