% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/impc.R
\name{impc_parse_phenotypes}
\alias{impc_parse_phenotypes}
\title{Turn an IMPC phenotype response into a table}
\usage{
impc_parse_phenotypes(body, mgi = NA_character_)
}
\arguments{
\item{body}{A parsed IMPC \code{genotype-phenotype/select} response.}

\item{mgi}{The MGI marker accession that was queried.}
}
\value{
A tibble of \code{mp_id}, \code{mp_name}, \code{allele}, \code{allele_symbol},
\code{zygosity}, and \code{source_url}, one row per distinct phenotype term. \code{NULL}
when there are none.
}
\description{
Pure.
}
\section{One row per term, not per observation}{

IMPC reports a document per phenotype, sex, zygosity, and parameter
combination, so the same term recurs many times over. Rows are collapsed to
distinct \code{mp_id}, keeping the first occurrence. Counting the raw documents
would report a gene's phenotype breadth several times over.

\code{mp_id} is an MP term for most phenotypes and an MPATH term for pathology
findings. Both appear in the same field.
}

\examples{
body <- list(response = list(docs = list(
  list(
    mp_term_id = "MP:0011100",
    mp_term_name = "preweaning lethality, complete penetrance",
    allele_accession_id = "MGI:4364806",
    zygosity = "homozygote"
  )
)))
impc_parse_phenotypes(body, "MGI:97306")

}
