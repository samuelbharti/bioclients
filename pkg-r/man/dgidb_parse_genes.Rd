% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dgidb.R
\name{dgidb_parse_genes}
\alias{dgidb_parse_genes}
\title{Turn a DGIdb genes response into a table}
\usage{
dgidb_parse_genes(body, symbols)
}
\arguments{
\item{body}{A parsed DGIdb GraphQL response body.}

\item{symbols}{The gene symbols that were queried, in the order asked.}
}
\value{
A tibble of \code{symbol}, \code{concept_id}, \code{interaction_count}, and
\code{source_url}, one row per entry in \code{symbols}.
}
\description{
Pure. Rows come back in \code{symbols} order, one per input.
}
\section{A real zero is not a miss}{

The distinction this parser exists to preserve. A gene DGIdb knows about with
no recorded interactions has \code{interaction_count = 0}: that is an answer, and
it means the gene is not currently druggable. A gene DGIdb has never heard of
has \code{interaction_count = NA}: that is an absence of evidence.

Collapsing the two would tell a caller that an unknown gene is known not to be
druggable, which is a different and much stronger claim than the data
supports.
}

\examples{
body <- list(data = list(genes = list(nodes = list(
  list(name = "NF1", conceptId = "hgnc:7765", interactions = list(list(), list()))
))))
dgidb_parse_genes(body, "NF1")

}
