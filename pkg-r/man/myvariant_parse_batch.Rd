% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/myvariant.R
\name{myvariant_parse_batch}
\alias{myvariant_parse_batch}
\title{Turn a MyVariant batch response into a table}
\usage{
myvariant_parse_batch(body, ids)
}
\arguments{
\item{body}{A parsed MyVariant batch response, a flat array.}

\item{ids}{The MyVariant ids that were requested, in order.}
}
\value{
A tibble with one row per entry in \code{ids}, plus an \code{id} column.
}
\description{
Pure. One row per requested id, in the order asked.
}
\section{notfound is an answer, not an error}{

MyVariant reports an unknown variant as HTTP 200 with \code{notfound: true}. That
is the source saying it has nothing, which is different from the call
failing. Such rows come back as \code{NA} rather than being dropped, so a caller
zipping by position stays aligned.
}

\examples{
body <- list(list(query = "chr17:g.7676154G>C", dbnsfp = list(
  genename = list("TP53"), cadd = list(raw_rankscore = 0.17)
)))
myvariant_parse_batch(body, "chr17:g.7676154G>C")

}
