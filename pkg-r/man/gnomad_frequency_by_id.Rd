% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gnomad.R
\name{gnomad_frequency_by_id}
\alias{gnomad_frequency_by_id}
\title{Population allele frequency for a variant, by id}
\usage{
gnomad_frequency_by_id(
  variant_id,
  dataset = GNOMAD_DATASET,
  reference_genome = "GRCh38",
  ...
)
}
\arguments{
\item{variant_id}{A gnomAD variant id, see \code{\link[=gnomad_variant_id]{gnomad_variant_id()}}.}

\item{dataset}{The gnomAD dataset. \code{gnomad_r4} is GRCh38; the \code{gnomad_r2}
datasets are GRCh37.}

\item{reference_genome}{The assembly the id is on. The variant query is
keyed by dataset alone, so this is checked against \code{dataset} and a
mismatch is refused rather than sent, because gnomAD would answer with
whatever sits at those coordinates on the other assembly.}

\item{...}{Passed to \code{\link[biohttp:post_json]{biohttp::post_json()}}.}
}
\value{
A biohttp envelope whose \code{data} is the one-row tibble described in
\code{\link[=gnomad_parse_variant]{gnomad_parse_variant()}}. \code{no_data} when gnomAD has no record of the
variant.
}
\description{
Looked up by \code{chrom-pos-ref-alt}, which names one allele exactly. This is
the lookup \code{\link[=gnomad_frequency]{gnomad_frequency()}} cannot do, because an rsID is shared by
every allele at a site.
}
\examples{
\dontrun{
biohttp::body_or_null(gnomad_frequency_by_id("17-7676154-G-C"))
}

}
