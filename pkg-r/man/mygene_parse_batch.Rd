% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mygene.R
\name{mygene_parse_batch}
\alias{mygene_parse_batch}
\title{Turn a MyGene batch response into a gene table}
\usage{
mygene_parse_batch(body, symbols)
}
\arguments{
\item{body}{A parsed MyGene batch response, a flat list of hit records.}

\item{symbols}{The identifiers that were queried, in the order asked.}
}
\value{
A tibble with one row per entry in \code{symbols}, same order.
}
\description{
Pure. The batch \verb{/query} POST returns a flat array where each element echoes
its input \code{query}. An ambiguous query yields several elements, best \verb{_score}
first, and an unmatched one yields an element with \code{notfound = true}.
}
\details{
Hits are grouped by the echoed query and resolved through \code{\link[=mygene_pick_hit]{mygene_pick_hit()}},
then mapped back onto \code{symbols} \strong{in input order}, so a caller zips the
result onto its input by position. An unmatched or invalid token yields a row
of \code{NA} rather than being dropped, because a shorter table would silently
shift every row after it.
}
\examples{
body <- list(
  list(query = "TP53", symbol = "TP53", entrezgene = "7157"),
  list(query = "NOPE", notfound = TRUE)
)
mygene_parse_batch(body, c("TP53", "NOPE"))

}
