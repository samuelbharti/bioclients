% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioclients-package.R
\docType{package}
\name{bioclients-package}
\alias{bioclients}
\alias{bioclients-package}
\title{bioclients: Clients for Biological Database Web Services}
\description{
\if{html}{\figure{logo.svg}{options: style='float: right' alt='logo' width='120'}}

Look up genes, variants and proteins from R, without writing a client for every biological web service. Each service gets one client that makes the request and returns a table. Parsing is a separate function that needs no network, so it can run on a saved response and be tested offline. Transport, retries, caching and error handling are left to the 'biohttp' package. Dependencies for single services are optional, so you do not install what you will not use. The services covered include 'Ensembl', described in Dyer et al. (2025) \doi{10.1093/nar/gkae1071}, 'UniProt', in The UniProt Consortium (2025) \doi{10.1093/nar/gkae1010}, 'gnomAD', in Chen et al. (2024) \doi{10.1038/s41586-023-06045-0}, 'Open Targets', in Buniello et al. (2025) \doi{10.1093/nar/gkae1128}, and the 'AlphaFold' Protein Structure Database, in Varadi et al. (2024) \doi{10.1093/nar/gkad1011}. Each client's help page cites the service it calls.
}
\details{
Every service module ships two halves, and the split is the point.

The \strong{client} builds a request, calls into \code{biohttp}, and returns its
envelope. It knows URLs, parameters, and rate limits, and it touches the
network. Start at \code{\link[=mygene_gene]{mygene_gene()}}, \code{\link[=gnomad_constraint]{gnomad_constraint()}},
\code{\link[=gnomad_frequency]{gnomad_frequency()}}, or \code{\link[=clinvar_classification]{clinvar_classification()}}.

The \strong{parser} takes an already-parsed response body and returns a canonical
structure. It is pure, it never touches the network, and it is what the
offline tests exercise. See \code{\link[=mygene_parse_hits]{mygene_parse_hits()}},
\code{\link[=gnomad_parse_constraint]{gnomad_parse_constraint()}}, and \code{\link[=clinvar_parse_record]{clinvar_parse_record()}}.

A caller that already has a response body can use a parser on its own.
}
\section{What comes back}{

Every client returns a \code{biohttp} envelope rather than raising, so a caller
branches on \code{res$status} and writes no \code{tryCatch()} of its own. Reach for
\code{biohttp::body_or_null()} when the reason for a failure is genuinely not
actionable.

Reaching a source that has nothing for a query is \code{no_data}, not an error.
It is an answer.
}

\section{Batching}{

\code{\link[=mygene_genes]{mygene_genes()}} and \code{\link[=gnomad_constraints]{gnomad_constraints()}} ask about many genes in one or a
few requests rather than one request per gene. The round trip is where
essentially all the time in these calls goes, so this is the difference that
matters. Both return rows in input order, one row per input, so a caller can
zip results onto its inputs by position.
}

\seealso{
Useful links:
\itemize{
  \item \url{https://github.com/samuelbharti/bioclients}
  \item \url{https://www.samuelbharti.com/bioclients/}
  \item Report bugs at \url{https://github.com/samuelbharti/bioclients/issues}
}

}
\author{
\strong{Maintainer}: Samuel Bharti \email{samuelbharti.io@gmail.com} (\href{https://orcid.org/0000-0003-4190-7058}{ORCID}) [copyright holder]

Authors:
\itemize{
  \item Samuel Bharti \email{samuelbharti.io@gmail.com} (\href{https://orcid.org/0000-0003-4190-7058}{ORCID}) [copyright holder]
}

Other contributors:
\itemize{
  \item Barret Schloerke \email{barret@posit.co} (\href{https://orcid.org/0000-0001-9986-114X}{ORCID}) [thesis advisor]
  \item Carson Sievert \email{carson@posit.co} (\href{https://orcid.org/0000-0002-4958-2844}{ORCID}) [thesis advisor]
  \item Posit Software, PBC (\href{https://ror.org/03wc8by49}{ROR}) [copyright holder, funder]
}

}
\keyword{internal}
