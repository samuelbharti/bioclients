% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/myvariant.R
\name{myvariant_variants}
\alias{myvariant_variants}
\title{Annotate many variants in one request}
\usage{
myvariant_variants(ids, assembly = "hg38", fields = MYVARIANT_FIELDS, ...)
}
\arguments{
\item{ids}{MyVariant ids. Build them with \code{\link[=myvariant_id]{myvariant_id()}}.}

\item{assembly}{The genome assembly. Changing this from \code{"hg38"} only makes
sense for genuinely GRCh37 coordinates.}

\item{fields}{The MyVariant fields to request.}

\item{...}{Passed to \code{\link[biohttp:post_json]{biohttp::post_json()}}, for example \code{throttle}.}
}
\value{
A biohttp envelope whose \code{data} is a tibble with one row per entry in
\code{ids}, in the same order. See \code{\link[=myvariant_parse_batch]{myvariant_parse_batch()}}.
}
\description{
Annotate many variants in one request
}
\section{assembly is not optional}{

\code{assembly = "hg38"} is always sent. Without it MyVariant answers 200 with
\code{notfound} for every GRCh38 variant, so a whole cohort disappears with no
error anywhere. There is no way to omit it through this function, and that is
deliberate.
}

\examples{
\donttest{
ids <- myvariant_id("17", 7676154, "G", "C")
biohttp::body_or_null(myvariant_variants(ids))
}

}
