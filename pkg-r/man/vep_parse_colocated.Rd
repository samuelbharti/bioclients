% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vep.R
\name{vep_parse_colocated}
\alias{vep_parse_colocated}
\title{Turn the colocated variants of a VEP element into a table row}
\usage{
vep_parse_colocated(element)
}
\arguments{
\item{element}{One parsed VEP result element.}
}
\value{
A one-row tibble of \code{rsid}, \code{gnomadg_af}, \code{gnomade_af},
\code{gnomadg_af_max}, \code{gnomade_af_max}, and \code{clin_sig}, all \code{NA} when the
element has no dbSNP record.
}
\description{
Pure. A VEP element lists the known variants at the same site under
\code{colocated_variants}: the dbSNP record, and COSMIC and HGMD entries beside
it. The dbSNP record is the one carrying population frequencies and
clinical significance, so that is the record read. When several dbSNP
records are listed, the one with frequencies wins.
}
\details{
Frequencies are only present when \code{af}, \code{af_gnomade} or \code{af_gnomadg} was
requested, see \code{\link[=vep_default_options]{vep_default_options()}}. \code{gnomadg_af_max} and
\code{gnomade_af_max} are the largest per-population frequency of that source,
which is what a rarity filter wants rather than the overall frequency.
\code{clin_sig} is the significance of the element's own allele where VEP
reports it per allele, so the alleles of a multi-allelic site do not share
one answer.
}
\examples{
element <- list(
  allele_string = "G/C",
  colocated_variants = list(list(
    id = "rs1042522",
    frequencies = list(C = list(gnomadg = 0.62, gnomadg_afr = 0.38)),
    clin_sig = list("benign")
  ))
)
vep_parse_colocated(element)

}
