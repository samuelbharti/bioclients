% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vep.R
\name{vep_default_options}
\alias{vep_default_options}
\title{The request flags VEP is asked for by default}
\usage{
vep_default_options()
}
\value{
A named list of flags.
}
\description{
Pure. The default for the \code{options} argument of \code{\link[=vep_variants]{vep_variants()}}. Start
from this and add to it rather than replacing it, because \code{vcf_string} is
one of the identities results are matched on and \code{mane} is what
\code{\link[=vep_pick_transcript]{vep_pick_transcript()}} chooses by.
}
\section{Supported flags}{

Every entry is a VEP request parameter, sent as \code{name=value} on the query
string. \code{TRUE} and \code{1} send \code{name=1}; \code{FALSE}, \code{0} and \code{NULL} omit the flag.
Names are case sensitive, exactly as VEP spells them. The flags this package
parses are:
\itemize{
\item \code{AlphaMissense}, \code{mane}, \code{numbers}, \code{vcf_string}: the defaults.
\item \code{af}, \code{af_gnomade}, \code{af_gnomadg}: colocated variant frequencies, read by
\code{\link[=vep_parse_colocated]{vep_parse_colocated()}}.
\item \code{CADD}, \code{SpliceAI}, \code{REVEL}: per-transcript predictor scores, read by
\code{\link[=vep_parse_element]{vep_parse_element()}}.
\item \code{hgvs}: \code{hgvsc} and \code{hgvsp} notation per transcript.
\item \code{canonical}: marks the canonical transcript.
\item \code{pick}, \code{pick_allele_gene}: ask VEP to return one transcript per variant
or per allele and gene, rather than all of them.
\item \code{protein}, \code{domains}, \code{variant_class}: extra transcript annotation, carried
through untouched in the response for a caller parsing it directly.
\item \code{LoF}: LOFTEE, read into the \code{lof} column.
}

\code{dbNSFP} is refused. It returns comma-joined multi-transcript strings in
dbNSFP's own order, not aligned to the transcript being reported, so the
values silently belong to a different transcript than the rest of the row.
}

\examples{
vep_default_options()
c(vep_default_options(), list(CADD = 1, REVEL = 1))

}
