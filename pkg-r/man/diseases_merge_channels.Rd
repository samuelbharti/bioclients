% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diseases.R
\name{diseases_merge_channels}
\alias{diseases_merge_channels}
\title{Combine DISEASES channels, keeping the strongest score per gene}
\usage{
diseases_merge_channels(tables)
}
\arguments{
\item{tables}{A list of tibbles from \code{\link[=diseases_parse_channel]{diseases_parse_channel()}}. \code{NULL}
entries are ignored.}
}
\value{
A tibble of \code{symbol}, \code{score}, and \code{channel}, ordered by score,
highest first. \code{channel} names the channel the winning score came from.
\code{NULL} when there is nothing to combine.
}
\description{
Pure.
}
\section{This is a combining convention, not the source's own answer}{

DISEASES scores each channel separately and does not publish a combined
figure. Taking the maximum treats curated and mined evidence as
interchangeable, which suits a screen looking for any support at all and
suits nothing that cares where the support came from. Use
\code{\link[=diseases_channel]{diseases_channel()}} and keep the channels apart if that distinction
matters.
}

\examples{
diseases_merge_channels(list(
  tibble::tibble(symbol = "NF1", protein = NA, score = 5, channel = "Knowledge"),
  tibble::tibble(symbol = "NF1", protein = NA, score = 2, channel = "Textmining")
))

}
\references{
Pletscher-Frankild et al. (2015). DISEASES: text mining and data
integration of disease-gene associations. Methods 74, 83-89.
\doi{10.1016/j.ymeth.2014.11.020}

Service documentation: \url{https://diseases.jensenlab.org/}
}
