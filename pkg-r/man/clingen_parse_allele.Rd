% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clingen.R
\name{clingen_parse_allele}
\alias{clingen_parse_allele}
\title{Turn one Allele Registry element into a table row}
\usage{
clingen_parse_allele(element)
}
\arguments{
\item{element}{One parsed Allele Registry element.}
}
\value{
A one-row tibble.
}
\description{
Pure.
}
\section{An unresolved input is a row, not a gap}{

ClinGen answers an input it could not resolve with an object carrying no
\verb{@id}, and an \code{errorType}/\code{message} instead. That becomes a row with
\code{resolved = FALSE} and the reason, rather than being dropped. Dropping it
would shift every later row onto the wrong variant, and would lose the reason
the input was rejected.
}

\examples{
clingen_parse_allele(list(errorType = "IncorrectHgvsPosition"))

}
