% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vep.R
\name{vep_variants_all}
\alias{vep_variants_all}
\title{Consequence predictions for any number of variants}
\usage{
vep_variants_all(
  chrom,
  pos,
  ref,
  alt,
  chunk_size = VEP_BATCH,
  options = vep_default_options(),
  ...
)
}
\arguments{
\item{chrom, pos, ref, alt}{Variant components, all the same length.}

\item{chunk_size}{Variants per request, at most \code{VEP_BATCH} (200).}

\item{options}{A named list of VEP request flags. See
\code{\link[=vep_default_options]{vep_default_options()}} for the supported flags and what each one adds to
the result.}

\item{...}{Passed to \code{\link[biohttp:post_json_many]{biohttp::post_json_many()}}, for example \code{throttle} or
\code{max_active}.}
}
\value{
A biohttp envelope whose \code{data} is a tibble with one row per
variant, in the order asked: the columns of \code{\link[=vep_parse_batch]{vep_parse_batch()}} plus
\code{status}, which is \code{"ok"} for a row whose chunk was answered and the
failing envelope's status otherwise.
}
\description{
\code{\link[=vep_variants]{vep_variants()}} refuses more than the 200 VEP accepts per POST. This
chunks the input at \code{chunk_size} and dispatches the chunks through
\code{\link[biohttp:post_json_many]{biohttp::post_json_many()}}, so a variant table of any length is a handful
of requests rather than a loop written at every call site.
}
\section{A failed chunk is rows of NA, not a failed call}{

Following \code{\link[=gnomad_constraints]{gnomad_constraints()}}. Each chunk's envelope is inspected on its
own; a chunk that failed yields a row of \code{NA} per variant carrying the
envelope status in \code{status}, while the chunks that succeeded are parsed as
usual. Partial failure is the normal case across many requests, and taking
the whole call down would throw away the answers that did arrive. Only
when every chunk failed is the first failing envelope returned, so an
outage still reads as one.
}

\examples{
\dontrun{
biohttp::body_or_null(vep_variants_all(
  c("7", "17"),
  c(140753336, 7676154),
  c("A", "G"),
  c("T", "C")
))
}

}
