% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gnomad.R
\name{gnomad_parse_variant}
\alias{gnomad_parse_variant}
\title{Turn a gnomAD variant response into a frequency row}
\usage{
gnomad_parse_variant(body, variant_id = NA_character_)
}
\arguments{
\item{body}{A parsed gnomAD GraphQL response body.}

\item{variant_id}{The id that was queried, carried through to the row.}
}
\value{
A one-row tibble of \code{variant_id}, \code{rsid}, \code{exome_af}, \code{exome_ac},
\code{exome_an}, \code{exome_nhomalt}, \code{genome_af}, \code{genome_ac}, \code{genome_an},
\code{genome_nhomalt}, \code{grpmax_af}, \code{grpmax_an}, \code{grpmax_id}, \code{faf95},
\code{faf95_pop}, and \code{filters}. \code{NULL} when the body carries no variant,
which is how gnomAD answers for a variant it has never seen.
}
\description{
Pure. The flat, one-row form of a variant record, for a variant table that
wants a frequency per row. \code{\link[=gnomad_parse_frequency]{gnomad_parse_frequency()}} is the nested form
with the per-ancestry table.
}
\section{grpmax is derived}{

The API does not serve a group maximum. It is computed here from the
per-group counts, exome and genome summed, leaving out the bottlenecked
groups and the remaining bucket the same way gnomAD does. See
\code{GNOMAD_GRPMAX_EXCLUDED}.
}

\examples{
body <- list(data = list(variant = list(
  variant_id = "17-7676154-G-C",
  rsids = list("rs1042522"),
  exome = list(af = 0.72, ac = 1046941, an = 1461558, homozygote_count = 380188)
)))
gnomad_parse_variant(body, "17-7676154-G-C")

}
