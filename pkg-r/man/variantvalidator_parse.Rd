% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/variantvalidator.R
\name{variantvalidator_parse}
\alias{variantvalidator_parse}
\title{Turn a VariantValidator response into a table}
\usage{
variantvalidator_parse(body, submitted = NA_character_)
}
\arguments{
\item{body}{A parsed VariantValidator response.}

\item{submitted}{The HGVS string that was sent.}
}
\value{
A one-row tibble of \code{resolved}, \code{submitted}, \code{normalized}, \code{gene},
\code{protein}, \code{chrom}, \code{pos}, \code{ref}, \code{alt}, and \code{warnings}, where \code{warnings} is
a list column. \code{NULL} when there is no record at all.
}
\description{
Pure.
}
\section{The response is keyed by the answer, not by a fixed name}{

VariantValidator returns an object whose key is the \strong{normalized} variant,
which is not known before the call. \code{flag} and \code{metadata} are the only fixed
keys, so the record is found by removing those rather than by looking up a
name. A parser expecting a fixed key finds nothing.
}

\examples{
body <- list(
  flag = "gene_variant",
  metadata = list(),
  `NM_000546.6:c.215C>G` = list(gene_symbol = "TP53")
)
variantvalidator_parse(body, "NM_000546.6:c.215C>G")

}
