% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mygene.R
\name{mygene_parse_hits}
\alias{mygene_parse_hits}
\title{Turn MyGene hits into a gene table}
\usage{
mygene_parse_hits(body, symbol = NA_character_)
}
\arguments{
\item{body}{A parsed MyGene \verb{/query} response, the whole body including
\code{hits}.}

\item{symbol}{The identifier that was queried, used to break the scoring tie
described in \code{\link[=mygene_pick_hit]{mygene_pick_hit()}} and as the fallback symbol.}
}
\value{
A one-row tibble with \code{symbol}, \code{name}, \code{summary}, \code{entrez},
\code{ensembl_gene}, \code{uniprot}, \code{hgnc}, and \code{type_of_gene}. \code{NULL} when the body
carries no usable hit.
}
\description{
Pure. Takes an already-parsed response body and never touches the network, so
it is tested directly against a stored response.
}
\examples{
body <- list(hits = list(list(
  symbol = "TP53",
  name = "tumor protein p53",
  entrezgene = "7157"
)))
mygene_parse_hits(body, "TP53")

}
\references{
Xin et al. (2016). High-performance web services for querying gene and
variant annotation. Genome Biology 17, 91.
\doi{10.1186/s13059-016-0953-9}

Service documentation: \url{https://mygene.info/}
}
