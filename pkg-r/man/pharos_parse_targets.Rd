% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pharos.R
\name{pharos_parse_targets}
\alias{pharos_parse_targets}
\title{Turn a Pharos targets response into a table}
\usage{
pharos_parse_targets(body, symbols)
}
\arguments{
\item{body}{A parsed Pharos GraphQL response body.}

\item{symbols}{The gene symbols that were queried, in the order asked.}
}
\value{
A tibble of \code{symbol}, \code{tdl}, and \code{source_url}, one row per entry in
\code{symbols}. \code{tdl} is one of \code{Tclin}, \code{Tchem}, \code{Tbio}, \code{Tdark}, or \code{NA}.
}
\description{
Pure. Rows come back in \code{symbols} order, one per input.
}
\details{
A TDL Pharos does not recognise is reported as \code{NA} rather than passed
through, so a caller never has to guess whether an unexpected string is a new
level or a typo.
}
\examples{
body <- list(data = list(targets = list(targets = list(
  list(sym = "NF1", tdl = "Tbio")
))))
pharos_parse_targets(body, "NF1")

}
