% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/protvar.R
\name{protvar_parse_population}
\alias{protvar_parse_population}
\title{Turn a ProtVar population response into a table}
\usage{
protvar_parse_population(body)
}
\arguments{
\item{body}{A parsed ProtVar \verb{/population} response.}
}
\value{
A tibble of \code{change} and \code{sources}, where \code{sources} is a
comma-separated list of distinct source names. \code{NULL} when there are no
variants.
}
\description{
Pure. Known variants at the residue.
}
\section{Sources are deduplicated}{

A variant carries one cross-reference per supporting record, so the same
source name recurs many times: the TP53 175 fixture lists \code{NCI-TCGA} four
times for a single change. Reporting them raw makes a single database look
like corroboration from several.
}

\examples{
body <- list(variants = list(list(
  alternativeSequence = "Cys",
  xrefs = list(list(name = "NCI-TCGA"), list(name = "NCI-TCGA"))
)))
protvar_parse_population(body)

}
