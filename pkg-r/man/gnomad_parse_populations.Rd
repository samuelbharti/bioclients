% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gnomad.R
\name{gnomad_parse_populations}
\alias{gnomad_parse_populations}
\title{Combine gnomAD per-ancestry counts into one frequency table}
\usage{
gnomad_parse_populations(exome_pops, genome_pops)
}
\arguments{
\item{exome_pops, genome_pops}{The \code{populations} lists from each sample set.}
}
\value{
A tibble of \code{pop}, \code{label}, \code{ac}, \code{an}, \code{af}, sorted by frequency
descending. \code{NULL} when there is nothing to report.
}
\description{
Pure. Sums the exome and genome sample sets per ancestry group.
}
\details{
Sex-split ids such as \code{nfe_XX} and the bare \code{XX}/\code{XY} breakdowns are dropped,
by keeping only the known ancestry codes, so the result is one row per
ancestry rather than a mix of ancestries and sexes.
}
\examples{
gnomad_parse_populations(
  list(list(id = "nfe", ac = 3, an = 1000)),
  list(list(id = "nfe", ac = 1, an = 500))
)

}
