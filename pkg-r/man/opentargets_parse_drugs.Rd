% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opentargets.R
\name{opentargets_parse_drugs}
\alias{opentargets_parse_drugs}
\title{Turn known-drug rows into a table}
\usage{
opentargets_parse_drugs(body)
}
\arguments{
\item{body}{A parsed Open Targets GraphQL response body.}
}
\value{
A tibble of \code{drug}, \code{drug_id}, \code{drug_type}, \code{max_phase}, \code{diseases},
\code{disease_ids}, and \code{source_url}. \code{NULL} when the target has no known drugs.
}
\description{
Pure.
}
\section{Every disease, not just the first}{

A drug row carries the whole list of diseases it has been tried against, and
the first entry is often the least useful one. In the stored BRAF response,
BELVARAFENIB lists five, and the first has no mapped \code{disease} at all. So the
diseases arrive as list columns rather than as one picked value.

\code{diseases} prefers the mapped disease name and falls back to
\code{diseaseFromSource}, which is the label the trial registry used. \code{disease_ids}
is \code{NA} in the positions Open Targets could not map.

\code{max_phase} is the raw \code{maxClinicalStage}, for example \code{"PHASE_2"}. Turning
that into "Phase 2" is presentation and belongs to whatever is presenting it.
}

\examples{
body <- list(data = list(target = list(
  drugAndClinicalCandidates = list(count = 1, rows = list(
    list(
      maxClinicalStage = "PHASE_2",
      drug = list(id = "CHEMBL1", name = "DRUGX", drugType = "Small molecule"),
      diseases = list(list(
        diseaseFromSource = "melanoma",
        disease = list(id = "MONDO_0005105", name = "melanoma")
      ))
    )
  ))
)))
opentargets_parse_drugs(body)

}
\references{
Buniello et al. (2025). Open Targets Platform: facilitating therapeutic
hypotheses building in drug discovery. Nucleic Acids Research 53(D1),
D1467-D1475. \doi{10.1093/nar/gkae1128}

Service documentation: \url{https://platform.opentargets.org/}
}
