% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensembl.R
\name{ensembl_parse_gene_model}
\alias{ensembl_parse_gene_model}
\title{Turn an Ensembl gene lookup into a gene model}
\usage{
ensembl_parse_gene_model(record)
}
\arguments{
\item{record}{A parsed \code{lookup/id} response fetched with \code{expand=1}.}
}
\value{
A list of \code{transcript}, \code{strand}, \code{region}, \code{gene_start},
\code{gene_end}, and \code{exons}, a tibble of \code{start}, \code{end}, and \code{number} sorted by
\code{start}. \code{NULL} when the record carries no transcript with exons.
}
\description{
Pure.
}
\section{Exon numbering follows the strand}{

Exons are sorted by genomic coordinate and then numbered in transcription
order. On the minus strand that means the highest-coordinate exon is exon 1.
Numbering by coordinate alone would put exon 1 at the wrong end of every
minus-strand gene.
}

\examples{
record <- list(
  seq_region_name = "17", start = 1, end = 900,
  Transcript = list(list(
    id = "ENST1", is_canonical = 1, strand = -1,
    Exon = list(list(start = 800, end = 900), list(start = 1, end = 100))
  ))
)
ensembl_parse_gene_model(record)$exons

}
\references{
Dyer et al. (2025). Ensembl 2025. Nucleic Acids Research 53(D1),
D948-D957. \doi{10.1093/nar/gkae1071}

Service documentation: \url{https://rest.ensembl.org/}
}
