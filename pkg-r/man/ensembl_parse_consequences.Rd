% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensembl.R
\name{ensembl_parse_consequences}
\alias{ensembl_parse_consequences}
\title{Turn Ensembl transcript consequences into a table}
\usage{
ensembl_parse_consequences(consequences)
}
\arguments{
\item{consequences}{The \code{transcript_consequences} array from a VEP record.}
}
\value{
A tibble of \code{gene}, \code{gene_id}, \code{transcript}, \code{biotype},
\code{consequence}, \code{impact}, \code{sift}, and \code{polyphen}. \code{NULL} when there are
none.
}
\description{
Pure.
}
\details{
Every consequence is returned, with its \code{biotype}, rather than only the
protein-coding ones. Which biotypes are worth showing is the caller's call,
and a filter here would hide the non-coding rows from a caller that wanted
them.
}
\examples{
ensembl_parse_consequences(list(list(
  gene_symbol = "BRAF",
  transcript_id = "ENST00000646891",
  biotype = "protein_coding",
  consequence_terms = list("missense_variant"),
  impact = "MODERATE"
)))

}
