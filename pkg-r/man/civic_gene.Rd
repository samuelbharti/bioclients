% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/civic.R
\name{civic_gene}
\alias{civic_gene}
\title{Curated clinical evidence counts for a gene}
\usage{
civic_gene(symbol, ...)
}
\arguments{
\item{symbol}{A HUGO gene symbol.}

\item{...}{Passed to \code{\link[biohttp:post_json]{biohttp::post_json()}}, for example \code{throttle}.}
}
\value{
A biohttp envelope whose \code{data} is a one-row tibble. See
\code{\link[=civic_parse_gene]{civic_parse_gene()}}.
}
\description{
Curated clinical evidence counts for a gene
}
\section{On the interpolated query}{

CIViC's \code{gene} query takes the symbol as an inline string rather than a
GraphQL variable, so the symbol is substituted into the query text. It is run
through this package's internal \code{clean_symbol()} first, which strips
everything outside \verb{[A-Za-z0-9._-]}. That is what stops a crafted symbol
closing the string and appending its own query. Do not remove it, and do not
switch to \code{paste0()} without it.
}

\examples{
\dontrun{
biohttp::body_or_null(civic_gene("NF1"))
}

}
