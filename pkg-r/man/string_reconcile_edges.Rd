% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/string.R
\name{string_reconcile_edges}
\alias{string_reconcile_edges}
\title{Rewrite edge endpoints back into the queried symbol space}
\usage{
string_reconcile_edges(edges, id_map)
}
\arguments{
\item{edges}{An edge tibble from \code{\link[=string_parse_network]{string_parse_network()}}.}

\item{id_map}{An identifier map from \code{\link[=string_parse_ids]{string_parse_ids()}}, or \code{NULL}.}
}
\value{
The edge tibble, with endpoints translated where a mapping existed.
}
\description{
Pure, and it exists because of a real and quiet failure.
}
\details{
STRING's canonical \code{preferredName} lags HGNC. Query \code{SEPTIN9} and the edges
come back naming \code{SEPT9}, and the \code{network} endpoint does \strong{not} echo the
query term. A caller matching edges against the symbols it asked about
therefore finds nothing for that gene and records it as having no partners,
which is indistinguishable from a real isolate.

Passing the map from \code{\link[=string_map_ids]{string_map_ids()}} through here translates the endpoints
back. An endpoint with no mapping is left alone, and an empty map leaves the
edges untouched, so reconciliation can never turn a good answer into a worse
one.
}
\examples{
edges <- tibble::tibble(gene_a = "SEPT9", gene_b = "TP53", score = 0.9)
map <- tibble::tibble(
  query = "SEPTIN9", preferred = "SEPT9", string_id = "9606.ENSP00000329125"
)
string_reconcile_edges(edges, map)

}
\references{
Szklarczyk et al. (2023). The STRING database in 2023: protein-protein
association networks and functional enrichment analyses for any sequenced
genome of interest. Nucleic Acids Research 51(D1), D638-D646.
\doi{10.1093/nar/gkac1000}

Service documentation: \url{https://string-db.org/}
}
