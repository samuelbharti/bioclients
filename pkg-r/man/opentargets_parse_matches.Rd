% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opentargets.R
\name{opentargets_parse_matches}
\alias{opentargets_parse_matches}
\title{Turn a disease search or lookup into a table}
\usage{
opentargets_parse_matches(body)
}
\arguments{
\item{body}{A parsed Open Targets GraphQL response body.}
}
\value{
A tibble of \code{id}, \code{name}, \code{score}, \code{description}, and \code{source_url},
best match first, or \code{NULL} when nothing matched. \code{score} is \code{NA} for a
direct lookup, which has no relevance score to report.
}
\description{
Pure, and it reads \strong{both} response shapes on purpose: the \code{search} query
returns a \code{hits} array, the direct \code{disease} lookup returns a single record.
A caller resolving a term does not know in advance which query ran, so one
parser reads either and the difference stops mattering downstream.
}
\examples{
hits <- list(data = list(search = list(hits = list(
  list(id = "MONDO_0018975", name = "neurofibromatosis type 1", score = 24.9)
))))
opentargets_parse_matches(hits)

single <- list(data = list(disease = list(id = "EFO_0000508", name = "neurofibroma")))
opentargets_parse_matches(single)

}
