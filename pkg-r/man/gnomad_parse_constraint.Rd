% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gnomad.R
\name{gnomad_parse_constraint}
\alias{gnomad_parse_constraint}
\title{Turn a gnomAD constraint response into a table}
\usage{
gnomad_parse_constraint(body, symbol = NA_character_)
}
\arguments{
\item{body}{A parsed gnomAD GraphQL response body.}

\item{symbol}{The gene symbol that was queried.}
}
\value{
A one-row tibble with \code{symbol}, \code{pli}, \code{loeuf}, \code{oe_lof}, \code{oe_mis},
\code{mis_z}, \code{syn_z}, and \code{lof_z}. \code{NULL} when the gene has no constraint
block, which is common and is an answer rather than a fault.
}
\description{
Pure. Takes an already-parsed response body and never touches the network.
}
\details{
\code{loeuf} is gnomAD's \code{oe_lof_upper}. The two names are the same number, and
the field is called \code{oe_lof_upper} in the API but LOEUF everywhere else,
including in gene-list-builder's ranking model. Both names appear here so a
reader of either can find it.
}
\examples{
body <- list(data = list(gene = list(
  gnomad_constraint = list(pli = 1, oe_lof_upper = 0.23)
)))
gnomad_parse_constraint(body, "BRAF")

}
