% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gnomad.R
\name{gnomad_frequencies}
\alias{gnomad_frequencies}
\title{Population allele frequency for many variants}
\usage{
gnomad_frequencies(
  variant_ids,
  dataset = GNOMAD_DATASET,
  reference_genome = "GRCh38",
  chunk_size = GNOMAD_CHUNK,
  ...
)
}
\arguments{
\item{variant_ids}{gnomAD variant ids, see \code{\link[=gnomad_variant_id]{gnomad_variant_id()}}.}

\item{dataset}{The gnomAD dataset. \code{gnomad_r4} is GRCh38; the \code{gnomad_r2}
datasets are GRCh37.}

\item{reference_genome}{The assembly the id is on. The variant query is
keyed by dataset alone, so this is checked against \code{dataset} and a
mismatch is refused rather than sent, because gnomAD would answer with
whatever sits at those coordinates on the other assembly.}

\item{chunk_size}{Variants per request.}

\item{...}{Passed to \code{\link[biohttp:post_json_many]{biohttp::post_json_many()}}.}
}
\value{
A biohttp envelope whose \code{data} is a tibble with one row per entry
in \code{variant_ids}, in the same order. See \code{\link[=gnomad_parse_variant]{gnomad_parse_variant()}}.
}
\description{
Batched through GraphQL aliases and chunked at \code{chunk_size} to stay under
gnomAD's query cost cap of 25, which was verified for this query. See
\code{GNOMAD_CHUNK}. Dispatched through \code{\link[biohttp:post_json_many]{biohttp::post_json_many()}}, so only
the chunks the cache is missing go over the wire.
}
\details{
A failed chunk yields a row of \code{NA} per variant rather than taking the
whole call down, following \code{\link[=gnomad_constraints]{gnomad_constraints()}}. A variant gnomAD has no
record of is a row of \code{NA} too, because that is an answer.
}
\examples{
\donttest{
biohttp::body_or_null(gnomad_frequencies(
  c("17-7676154-G-C", "7-117559590-ATCT-A")
))
}

}
\references{
Chen et al. (2024). A genomic mutational constraint map using variation
in 76,156 human genomes. Nature 625(7993), 92-100.
\doi{10.1038/s41586-023-06045-0}

Service documentation: \url{https://gnomad.broadinstitute.org/}
}
